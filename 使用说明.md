# 钢琴扒谱机 · 使用说明

## 一分钟上手

1. **解压**发布包到任意英文或中文路径（避免放在需要管理员权限的目录）。
2. 双击 **`安装环境.bat`**，等待完成（首次可能 10–40 分钟，视网速而定）。
3. 若要用 **MuScriptor**：
   - 打开 https://huggingface.co/MuScriptor/muscriptor-small （以及你要用的 medium/large）接受许可
   - https://huggingface.co/settings/tokens 创建 **Read** Token
   - 用记事本打开项目里的 **`.env`**，填写：
     ```text
     HF_TOKEN=hf_你的token
     ```
4. 双击 **`启动.bat`**，浏览器进入 http://127.0.0.1:8765
5. 下载或上传音频 → 选模型 → 开始扒谱 → 在 `output/` 取 MIDI

## 两个模型怎么选

| 模型 | 特点 | 注意 |
|------|------|------|
| **Aria-AMT** | 纯钢琴专用，本机权重已带好 | 需要 NVIDIA GPU |
| **MuScriptor** | 多乐器，可约束钢琴/弦乐/鼓等 | 需 HF Token；首次下权重；4GB 显存建议 **small** |

乐器约束示例：

- 纯钢琴：`acoustic_piano`
- 钢琴+小提琴+鼓：`acoustic_piano,violin,drums`
- 弦乐组：`string_ensemble`

## 目录说明

| 路径 | 作用 |
|------|------|
| `musicdata/` | 下载/上传的音频 |
| `output/` | 输出的 MIDI |
| `本地aria-amt/` | Aria 引擎与钢琴权重 |
| `本地muscriptor/` | MuScriptor 包装脚本（权重首次自动缓存） |
| `yt-dlp_win_x86/` | 下载器 |
| `.env` | 密钥（**不要发给别人**） |

## 常见问题

**安装失败 / CUDA unavailable**  
更新 NVIDIA 驱动；确认 `nvidia-smi` 能在命令行运行。

**MuScriptor 报 gated / 403**  
Token 有效但仍要在对应模型页点接受许可；small / medium / large 要分别接受。

**显存不足**  
MuScriptor 改用 `small`；或不要与其它占 GPU 的程序同时跑。

**端口占用**  
关闭已打开的扒谱机窗口，或结束占用 `8765` 的进程后再启动。

## 重新安装

删掉以下目录后，再运行 `安装环境.bat`：

- `runtime\.venv`
- `本地aria-amt\.venv`
- `本地muscriptor\.venv`
- `runtime\.installed`
